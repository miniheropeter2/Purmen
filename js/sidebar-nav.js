(() => {
  'use strict';
  // app.js owns navigation and mobile drawer behavior. Do not register a second
  // click listener here; duplicate capture/bubble handlers made the UI toggle
  // twice and appear unclickable.
})();
